# Town-in-a-Box-CORE Environment Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# Server Configuration
# =============================================================================

# Port to run the HTTP server on
PORT=3000

# CORS origin (use * for development, specific origins for production)
CORS_ORIGIN=*

# Rate limiting (requests per minute per IP)
RATE_LIMIT_MAX=100

# =============================================================================
# Database Configuration
# =============================================================================

# PostgreSQL connection string
# Format: postgresql://user:password@host:port/database
# Leave unset to use in-memory storage (development mode)
# DATABASE_URL=postgresql://postgres:password@localhost:5432/towninabox

# =============================================================================
# AI Configuration
# =============================================================================

# AI Provider: 'openai' or 'mock'
AI_PROVIDER=mock

# OpenAI API Key (required when AI_PROVIDER=openai)
# AI_API_KEY=sk-your-openai-api-key-here

# Optional: Override base URL for OpenAI-compatible APIs
# AI_BASE_URL=https://api.openai.com/v1

# Default model to use
AI_DEFAULT_MODEL=gpt-4.1-mini

# Sampling temperature (0.0-2.0, lower = more deterministic)
AI_DEFAULT_TEMPERATURE=0.3

# Maximum tokens for AI responses
AI_DEFAULT_MAX_TOKENS=2000

# =============================================================================
# Notification Configuration
# =============================================================================

# Email transport configuration
# Options: 'console' (logs to console), or SMTP configuration string
# Leave as 'console' for development
EMAIL_TRANSPORT=console

# SMTP configuration (when using SMTP transport)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-smtp-user
# SMTP_PASS=your-smtp-password
# SMTP_FROM=noreply@example.com

# =============================================================================
# Security Configuration (Production)
# =============================================================================

# JWT secret for authentication (generate a strong random string)
# JWT_SECRET=your-super-secret-jwt-key-at-least-32-characters

# Session secret (generate a strong random string)
# SESSION_SECRET=your-super-secret-session-key

# =============================================================================
# Logging Configuration
# =============================================================================

# Log level: 'debug', 'info', 'warn', 'error'
LOG_LEVEL=info

# Enable JSON logging format (for production log aggregation)
# LOG_FORMAT=json
